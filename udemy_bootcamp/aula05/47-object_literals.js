const fitBitData = {
    totalSteps       : 308727,
    totalMiles       : 211.7,
    avgCalorieBurn   : 5755,
    workoutsThisWeek : '5 of 7',
    avgGoodSleep     : '2:13'
};

console.log(fitBitData);

//we can access information using the dot syntax
console.log(fitBitData.totalMiles);

//when we make an object, the keys are automatically converted to strings
