//ARRAYS ARE INDEXED

let colors = ["red", "orange", "blue", "black", "purple"]

console.log(colors.length)
console.log(colors[0])

//to get the last one
console.log(colors[colors.length - 1])

//we get undefined if we try to access something that is not there
console.log(colors[123])